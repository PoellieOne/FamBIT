---
interface Props {
  title: string;
  body: string;
}

const { title, body } = Astro.props;
---

<section aria-label={title} class="c-trust-panel">
  <h2 class="c-trust-title">{title}</h2>
  <p class="c-trust-copy">{body}</p>
</section>
