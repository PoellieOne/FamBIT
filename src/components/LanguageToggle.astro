---
import { localizedPath, type Locale } from "../lib/i18n";

interface Props {
  active: Locale;
  basePath: string;
  label: string;
  language: {
    en: string;
    es: string;
  };
}

const { active, basePath, label, language } = Astro.props;
---

<nav aria-label={label} class="c-lang-toggle">
  <a
    href={localizedPath("en", basePath)}
    hreflang="en"
    aria-current={active === "en" ? "page" : undefined}
    class="c-lang-link"
  >
    {language.en}
  </a>
  <a
    href={localizedPath("es", basePath)}
    hreflang="es"
    aria-current={active === "es" ? "page" : undefined}
    class="c-lang-link"
  >
    {language.es}
  </a>
</nav>
